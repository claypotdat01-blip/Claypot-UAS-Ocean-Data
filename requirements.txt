cat > /home/claude/lautan/requirements.txt << 'ENDOFFILE'
# ============================================================
# LAUTAN — Requirements
# Instalasi semua sekaligus:
#   pip install -r requirements.txt
# ============================================================

# ── Core Streamlit & Data ──────────────────────────────────
streamlit>=1.32.0
pandas>=2.0.0
numpy>=1.26.0
plotly>=5.20.0

# ── HTTP / Networking ──────────────────────────────────────
requests>=2.31.0

# ── Sumber Data API ────────────────────────────────────────

# CMEMS (Copernicus Marine) ────────────────────────────────
# Digunakan untuk:
#   - Arus laut      : cmems_mod_glo_phy-cur_anfc_0.083deg_P1D-m
#   - SST            : cmems_mod_glo_phy-thetao_anfc_0.083deg_P1D-m
#   - Salinitas      : cmems_mod_glo_phy-so_anfc_0.083deg_P1D-m
#   - Klorofil-a     : cmems_obs-oc_glo_bgc-plankton_my_l4-gapfree-multi-4km_P1D
#                      (GANTI dari NASA MODIS)
copernicusmarine>=1.2.0

# ERA5 / ECMWF (angin) ─────────────────────────────────────
cdsapi>=0.6.1

# NetCDF4 (baca file .nc dari ERA5) ────────────────────────
netCDF4>=1.6.5

# NASA Earthdata ────────────────────────────────────────────
# TIDAK LAGI DIGUNAKAN untuk klorofil-a.
# Dipertahankan untuk kompatibilitas / keperluan lain.
# earthaccess>=0.9.0

# ── Machine Learning (Prediksi) ────────────────────────────
# Prophet (Facebook/Meta) — forecast deret waktu
prophet>=1.1.5

# ── Land Mask (filter daratan dari peta) ──────────────────
global-land-mask>=0.1.2

# ── Utilitas ──────────────────────────────────────────────
python-dotenv>=1.0.0
ENDOFFILE
echo "Done"
